{
  "name": "@postum/json-schema",
  "version": "0.0.1",
  "description": "Content types for Postum",
  "main": "index.js",
  "author": "Ezra Weller",
  "license": "MIT",
  "scripts": {
    "build-all": "yarn build-validate && yarn build-types",
    "build-validate": "node scripts/validate.build.js",
    "build-types": "node scripts/tsTypes.build.js",

    "test": "ts-node test/*"
  },
  "devDependencies": {
    "ajv": "^8.6.2",
    "json-schema-to-typescript": "^10.1.4",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
