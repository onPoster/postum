{
  "name": "@postum/json-schema",
  "version": "0.0.1",
  "description": "Content types for Postum",
  "main": "index.js",
  "author": "Ezra Weller",
  "license": "MIT",
  "scripts": {
    "generate-all": "yarn generate-validate && yarn generate-types",
    "generate-validate": "node scripts/generateValidate",
    "generate-types": "node scripts/generateTsTypes",

    "test": "ts-node test/*"
  },
  "devDependencies": {
    "ajv": "^8.6.2",
    "json-schema-to-typescript": "^10.1.4",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
