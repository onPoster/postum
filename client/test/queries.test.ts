/**
 * TODO spec tests:
 * 
 * Generally: start with a list of entities, then create each of those entities, 
 * then query that entire list and compare
 * 
 * Ideally also test delete cases, i.e. if I create a forum, create a bunch of threads and posts in that forum,
 * delete the forum, then query the threads or posts, do I get errors?
 * 
 * queries
 * - 
 * 
 */